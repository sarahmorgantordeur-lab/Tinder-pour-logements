{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:login": "cypress run --spec 'cypress/e2e/authLogin.cy.js'",
    "cy:run:register": "cypress run --spec 'cypress/e2e/authRegister.cy.js'"
  },
  "devDependencies": {
    "cypress": "^15.10.0",
    "wait-on": "^8.0.1"
  }
}
